# Local defaults. Render overrides both values through the Docker entrypoint.
server.port=${PORT:8080}
ai.fastapi.url=${AI_FASTAPI_URL:http://localhost:8000/predict}
spring.application.name=grade-change-backend
logging.level.com.paper.gradechange=INFO